# 🚀 Публікація на GitHub

Покрокова інструкція для завантаження проекту на GitHub.

## Передумови

1. Встановлений [Git](https://git-scm.com/downloads)
2. Акаунт на [GitHub](https://github.com)
3. Налаштований Git з твоїми даними:

```bash
git config --global user.name "Твоє Імʼя"
git config --global user.email "твоя@пошта.com"
```

## Крок 1: Створи репозиторій на GitHub

1. Зайди на [github.com](https://github.com)
2. Натисни **"+"** → **"New repository"**
3. Заповни:
   - **Repository name:** `delivery-analytics`
   - **Description:** `Система аналітики доставок курʼєрів`
   - **Public** або **Private** — на твій вибір
   - ❌ НЕ вмикай "Add a README file" (у нас вже є)
4. Натисни **"Create repository"**

## Крок 2: Ініціалізуй локальний репозиторій

Відкрий термінал в папці проекту і виконай:

```bash
# Перейди в папку проекту
cd delivery-analytics

# Ініціалізуй Git
git init

# Додай всі файли
git add .

# Зроби перший коміт
git commit -m "Initial commit: MVP delivery analytics dashboard"
```

## Крок 3: Зв'яжи з GitHub

```bash
# Заміни YOUR_USERNAME на свій GitHub username
git remote add origin https://github.com/YOUR_USERNAME/delivery-analytics.git

# Перейменуй гілку в main (якщо потрібно)
git branch -M main

# Завантаж на GitHub
git push -u origin main
```

### Якщо запитує пароль

GitHub більше не приймає паролі. Потрібен Personal Access Token:

1. Зайди в GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Натисни "Generate new token (classic)"
3. Дай назву, обери термін дії
4. Вибери scope: `repo` (повний доступ до репозиторіїв)
5. Натисни "Generate token"
6. Скопіюй токен і використовуй його замість пароля

## Крок 4: Перевір

1. Зайди на `https://github.com/YOUR_USERNAME/delivery-analytics`
2. Перевір що всі файли завантажились
3. README.md має відображатись внизу

## 🔄 Подальша робота

### Зберегти зміни

```bash
git add .
git commit -m "Опис змін"
git push
```

### Отримати зміни

```bash
git pull
```

### Подивитись статус

```bash
git status
```

## 📁 Структура що має бути на GitHub

```
delivery-analytics/
├── .gitignore
├── README.md
├── index.html
├── database/
│   ├── schema.sql
│   └── seed.sql
├── docs/
│   ├── GITHUB_SETUP.md
│   └── SUPABASE_SETUP.md
├── public/
└── src/
    ├── app.js
    ├── components/
    ├── config/
    │   └── supabase.example.js
    ├── services/
    │   ├── excel-parser.service.js
    │   └── supabase.service.js
    ├── styles/
    │   └── main.css
    └── utils/
        └── helpers.js
```

## 🌐 Опціонально: GitHub Pages

Можеш безкоштовно захостити статичну версію:

1. Зайди в Settings репозиторію
2. Знайди "Pages" в меню зліва
3. Source: "Deploy from a branch"
4. Branch: `main`, folder: `/ (root)`
5. Натисни "Save"
6. Через пару хвилин сайт буде доступний на `https://YOUR_USERNAME.github.io/delivery-analytics`

**⚠️ Увага:** На GitHub Pages працюватиме тільки localStorage версія, бо Supabase ключі не можна публікувати.

## 🔐 Важливо про безпеку

Ніколи не комітай:
- `src/config/supabase.js` (з реальними ключами)
- `.env` файли
- Будь-які API ключі чи паролі

Перевір що `.gitignore` містить ці файли!
